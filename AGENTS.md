# AGENTS.md

This repository contains a Codex skill. Keep the installable skill self-contained under `skills/video-master/`.

When editing the skill:

- Keep `SKILL.md` concise and workflow-oriented.
- Put reusable schemas, prompt patterns, and checklists in `skills/video-master/references/`.
- Validate the skill with the Codex skill validator before claiming it is ready.
- Sync changes to `~/.codex/skills/video-master` when the user wants the local Codex install updated.

